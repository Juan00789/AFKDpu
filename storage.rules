
service firebase.storage {
  match /b/{bucket}/o {
    // Users can only read any avatar, but only write to their own folder.
    match /avatars/{userId}/{allPaths=**} {
      allow read;
      allow write: if request.auth.uid == userId;
    }
  }
}
