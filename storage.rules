rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to all authenticated users for now.
    // This is for development and should be secured before production.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
